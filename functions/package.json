{
  "name": "kappacard-functions",
  "description": "Stripe checkout + webhook for Kappa Card Basic unlocks",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "type": "commonjs",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.3.2",
    "stripe": "^18.0.0"
  }
}
